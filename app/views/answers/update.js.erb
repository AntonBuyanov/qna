$('.answer-errors').html('<%= render 'shared/errors', resource: @answer %>');

<% unless @answer.errors.present? %>
  $('#answer-<%= @answer.id %>').html('<%= j render @answer %>');
  loadGist($('#answer-<%= @answer.id %> .gist'));
<% end %>
